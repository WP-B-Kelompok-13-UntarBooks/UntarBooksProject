
      <main>
        <div class="title-page">Book Detail</div>
        <hr>
        <div class="card-body">
            <div class="row">
              <div class="detail-col">
                <%   if (book.volumeInfo.imageLinks && book.volumeInfo.imageLinks.thumbnail) { %>
                  <img src="<%- book.volumeInfo.imageLinks.thumbnail %>" class="detail-card-img">
                <% } %>
                
                <% if (book.volumeInfo && book.volumeInfo.industryIdentifiers) { %>
                <% book.volumeInfo.industryIdentifiers.forEach((identify) => { %>
                  <p class="card-text-1"><%- identify.type %>: <%- identify.identifier %></p>
                <% }) %>
                <% } %>
                <p class="card-text-1">Length : <%- book.volumeInfo.pageCount %> pages</p>
                <p class="card-text-1">Author: <%- book.volumeInfo.authors[0] %> (<%- book.volumeInfo.publishedDate %>)</p>
              </div>
              
              <div class="col">
                <h4 class="card-title"><%- book.volumeInfo.title %></h4>
                <hr>
                <p class="card-text-detail"><%- book.volumeInfo.description %></p>

                <button type="button" class="btn btn-light btn-lg"><a href="https://books.google.co.id/books?id=<%- book.id %>&printsec=frontcover#v=onepage&q&f=true">Preview</a></button>
                <button type="button" class="btn btn-light btn-lg"><a href="#"><i id="fav" data-feather="heart"></i></a></button>
              </div>
            </div>
          </div>
    </main>
</body>